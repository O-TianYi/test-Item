# 标识是最顶层的EditorConfig配置文件
root = true

[*]
charset =  = utf-8
indent_style = space # 缩进风格
indent_size = 2 #缩进大小
end_of_line = lf # 控制换行类型
trim_trailing_whitespace = true # 去除行首的任意空白字符
insert_final_newline = true # 始终在文件末是插入一个新行

[*.md]
max_line_length = off 
trim_trailing_whitespace = false